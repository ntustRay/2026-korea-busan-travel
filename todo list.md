# UI 改版 TODO

這份清單是後續前端設計與操作改善的單一 backlog。先收集與排序需求，確認整體方向後再分批實作。

## P0：操作阻礙

- [x] 讓內容頁的返回控制在長頁面中持續可見
  - 問題：進入注意事項或更多資料的文章後，返回按鈕只在頁面頂端；讀到底部時必須捲回最上方。
  - 方向：優先評估 `position: sticky` 或 `position: fixed`；不要只使用 `absolute`，因為它不會隨捲動保持可見。
  - 驗收：在長文章的任意位置都能返回清單；不遮住正文、底部導覽或手機 safe area；鍵盤與螢幕閱讀器可操作。
  - 僅獨立內容頁顯示固定左上角的 `ArrowLeft + 返回`；首頁不顯示。
  - 瀏覽器無上一頁時返回行程首頁。

- [x] 修正底部導覽遮住頁面操作
  - 問題：Pixel 7 首屏中，固定導覽列會壓住下一步卡片內的次要按鈕。
  - 驗收：320px 以上寬度與 200% zoom 下，所有按鈕都能完整顯示及點擊；頁面內容保留足夠的底部安全距離。

## P1：視覺系統

- [x] 建立跨晴天／雨天一致的色彩角色
  - 固定一個主要互動色，NAVER Map、目前分頁與其他主要操作不可隨天氣改變語意。
  - 晴雨模式只調整背景、surface、文字與天氣狀態色。
  - 移除同一區塊內兩個大面積高彩度顏色互搶的情況。
  - 相同層級的區塊使用相同背景與邊界規則，不讓個別卡片任意換底色。
  - 驗收：每個顏色都能說明唯一用途；文字與操作達到 WCAG AA 對比。

- [x] 重新設計晴天模式
  - 目標：溫暖、放鬆，但不能像餐飲、美妝或通用活動 App。
  - 避免整頁米黃、暖色卡片堆疊及 magenta／藍色同時搶主角。
  - 保留釜山海洋城市識別，但以少量、語意清楚的方式使用。

- [x] 以 `lucide-react` 建立全站 icon system
  - 網頁顯示內容移除所有 Emoji 與 `◉`、`▤`、`!`、`•••` 等文字符號；原始筆記保留原文。
  - 導覽與重要操作使用 `Icon + 文字`；只有關閉、搜尋等慣用操作可單獨使用 icon，且必須有無障礙名稱。
  - 同一功能固定使用同一 icon，並統一尺寸、stroke、視覺重量與對齊方式。
  - 一個資訊區塊最多一個主 icon；一般條列不逐項塞 icon，純裝飾 Emoji 直接刪除。
  - icon 不使用彩色底框；重要程度以排序、留白及粗細表達。

- [x] 建立只屬於釜山旅行工具的視覺語彙
  - 方向候選：地鐵站牌、交通時刻板、車票或海岸路線。
  - 只選一個主要語彙，不混用多套裝飾風格。
  - 驗收：拿掉「釜山」文字後，仍能從結構與細節辨認它是韓國城市交通旅行工具。

- [x] 先研究成熟旅遊筆記網站，再決定新版設計
  - 實作前比較至少 3 個成熟旅遊筆記或行程產品的手機版。
  - 記錄它們如何安排首屏、圖片、行程、注意事項、長文與返回控制。
  - 只採用符合本專案「無腦放鬆、旅途中快速判斷」目的的模式，不直接照抄外觀。
  - 驗收：每個主要設計決定都能指出參考模式與採用原因，不憑空設計。
  - Wanderlog：採用「每日行程＋地圖／導航入口」和離線行程，不採用協作、推薦與預算功能。
  - TripIt：採用單一權威行程與依時間排列，不加入信件匯入與帳號。
  - Polarsteps：採用大路線圖與旅程總覽，不加入社群動態與旅遊回顧。

- [x] 重新設計 timeline，減少通用圓角卡片堆疊
  - 強化時間、目前位置、下一站與已略過狀態的資訊層級。
  - 避免每一筆都以相同卡片與相同按鈕呈現。

## P1：操作層級

- [x] 繼續收斂下一步卡片的操作
  - NAVER Map 保持唯一主要導航操作。
  - Uber、k.ride 與回飯店應有穩定且可預測的次要層級。
  - 晴天與雨天的相同操作需維持相同視覺語意。

- [x] 降低重複「設為目前行程」按鈕的噪音
  - 移除每項重複出現的按鈕，改為點擊整個行程項目並顯示短暫確認。
  - 預設依韓國時間推算目前行程；手動選擇後保持手動模式，直到使用者恢復自動判斷。
  - 手動狀態與晴雨模式使用 `localStorage` 保存；短暫展開與搜尋狀態使用 `sessionStorage`。
  - 目前狀態不可只靠顏色區分，下一步與略過操作放在目前項目附近。

## P1：資訊架構與內容呈現

- [x] 分離網頁快速操作與完整筆記
  - 網頁只呈現旅途中立即需要的摘要、下一步與操作入口。
  - 完整背景資料、長篇說明與補充資訊留在筆記頁，不直接堆進主要操作畫面。
  - 驗收：使用者不閱讀長文，也能在主畫面快速判斷現在要做什麼。
  - 未整理的原始資料預設不顯示；只有明確標記為「網頁顯示」的內容可發布。
  - 每則發布內容指定為全域、當日、下一步或參考；建置時檢查遺漏與重複，失敗時中止建置。

- [x] 依照手機視線掃描順序安排資訊
  - 最重要資訊放在首屏、區塊起點與視線最先停留的位置。
  - 先呈現「現在做什麼」，再呈現時間／地點，最後才是補充說明與次要操作。
  - 不把重要資訊放在長段落中間、卡片底部或需要多次捲動才看到的位置。
  - 驗收：快速掃過頁面時，能依序讀到目前狀態、下一步與主要操作。
  - 首屏順序：韓國日期時間、精簡天氣列、現在／下一步、時間、目的地、交通、唯一重要提醒、NAVER Map。
  - 開啟後 3 秒內能回答「現在要做什麼」，且上述行動資訊不需捲動即可看到。

- [x] 壓縮主要畫面的文字量
  - 每個區塊只保留一個重點，移除不影響當下決策的描述。
  - 標題、說明與按鈕不可重複表達相同資訊。
  - 優先使用短句、動詞與可掃讀的時間／地點資訊。

- [x] 將重要長篇說明預設收合
  - 非常重要的詳細說明可以保留，但使用 `details/summary` 或等效 collapse，預設關閉。
  - 收合標題先寫結論或警告，展開後才顯示原因、步驟與例外。
  - 不重要、與現場決策無關或重複的資訊直接移除，不因為已有內容就全部保留。
  - References 與來源連結移出主要閱讀流程；有保留價值時集中放入預設收合區。
  - 驗收：未展開時仍能完成主要任務；展開控制有清楚名稱、鍵盤操作與狀態語意。
  - 標題 16 字內直接顯示；80 個中文字內且只有一段的說明使用行內收合。
  - 超過 80 字，或包含多段、圖片、步驟、表格、references 時，才進入獨立內容頁。

- [x] 將注意事項改成現場可掃讀的行動清單
  - 一行只講一件事，不在同一項補背景、原因或例外。
  - 直接使用「請、不要、不用」等行動語句，不寫文章摘要。
  - 保留使用者原本的口吻，不擴寫成完整段落。
  - 高分基準：
    1. 吃完飯請立即移動！
    2. 飲料不用自己拿
    3. 信用卡不用撿
    4. 手機佔位子
    5. 地鐵博愛座潛規則
    6. 不要「嗯嗯」
    7. 不要亂拍人
    8. 不要用手指指人
  - 驗收：手機首屏可看到多項提醒；每項不換行或最多兩行；點開後才顯示完整解釋。
  - 已將文化、交通、SPA 與市場筆記整理成 37 條短指令；原始長文、搜尋與未整理筆記不公開。
  - 出發前 Checklist 與必裝 App 已移到獨立「行前」分頁。
  - 已加入分類 TOC，可直接跳到各事項大標；不可犯的超級紅線使用紅字與淡紅底。
  - 用語修正為「不要『嗯嗯』，要說『內內』！」。

- [x] 整併重複或相近內容
  - 相同提醒、交通方式與操作只保留一個權威入口。
  - 將晴雨模式、完整行程與注意事項中的類似內容整理成共用摘要，避免多處維護不同版本。
  - 驗收：同一件事不在同一畫面重複出現；內容來源與修改位置清楚。

- [x] 讓圖片成為主要視覺內容
  - 景點與路線圖片使用接近全寬的大尺寸呈現，不使用零碎縮圖牆。
  - 每張圖片都必須協助辨認地點、路線或下一步，不加入純裝飾圖片。
  - 驗收：手機首屏的圖片清楚、不模糊、不裁掉重要內容，並具備正確 `alt` 與離線載入能力。
  - 每個主要目的地最多一張辨識圖，置於下一步卡片後、詳細行程前，不可把行動資訊推出首屏。
  - 使用 WebP／AVIF 與手機合適尺寸；首張優先載入，其餘延遲載入，失敗時不顯示破圖。
  - 交通路線圖可點擊放大為全螢幕，支援關閉按鈕與 Esc。

- [x] 統一全站字體與字型規則
  - 標題、內文、時間與操作標籤使用固定且有限的字體角色。
  - 中文、韓文與英文混排時維持一致的字重、行高與視覺尺寸。
  - 不在不同卡片或分頁任意更換字體風格。
  - 最重要的事情使用粗體大標，並以整個標題或整個資訊區塊為單位調整。
  - 同一段文字內不突然改變顏色或字級；句中強調只能改變字重。
  - 使用系統無襯線字型，不依賴線上字型；只使用 `400 / 600 / 700` 三種字重。
  - 手機正文以 18px 為基準，小型標籤仍需清楚可讀；重要操作不得使用過小文字。

- [x] 修正需要垂直置中的元件
  - 天氣列、時間標籤、操作按鈕、狀態標籤與底部導覽使用明確的 `align-items`／`justify-content`。
  - 驗收：圖示與文字基線穩定，雙欄內容在高度不同時仍維持視覺置中。

- [x] 新增精簡天氣列
  - 放在首頁韓國日期時間下方、現在／下一步卡片上方，不建立大型天氣卡片。
  - 以 icon 快速表達天氣狀態，並保留短文字及溫度，不能只靠 icon 傳意。
  - 今天為主：狀態、目前溫度、最高／最低溫；明天為次：狀態與最高／最低溫。
  - 明天只有大雨、明顯降溫等會影響準備時，才增加一則粗體提醒。
  - 明確標示地點為釜山，並顯示濕度與紫外線；不顯示風速與日出，離線顯示最後資料及更新時間。
  - 天氣不可兼作晴雨行程切換。
  - 晴天／雨天行程使用獨立按鈕，明確顯示目前模式與切換結果。

- [x] 收斂底部導覽為四個入口
  - 固定保留「今天、行程、注意、行前」，皆使用 `Icon + 短文字`。
  - 移除「更多」；低頻功能移到對應頁面，搜尋保留在頁面上方。
  - 目前頁面不可只靠顏色標示，並保留 safe area 與內容底部空間。

- [x] 顯示釜山旅程需要的 App
  - 只公開整理後的 5 個必裝與 2 個安全選配 App。
  - 每個項目顯示名稱、用途與「開啟／安裝」操作。
  - Android 優先嘗試開啟 App；未安裝時回到對應 Google Play 頁面。
  - App 安裝狀態與 14 項行前 Checklist 可點擊勾選，並用 `localStorage` 保留。

## P2：內容與細節

- [x] 調整出發前首屏資訊順序
  - 倒數不可壓過真正的「下一步」。
  - 完整移除 PWA 安裝提示區塊，但保留 PWA 與離線能力。

- [x] 確保純 Static 與離線行程可靠
  - 不新增帳號、後端或跨裝置同步；老婆手機以查看為主，每支手機狀態彼此獨立。
  - 今天、完整行程、重要筆記、飯店地址與目的地韓文名稱必須離線可讀或複製。
  - NAVER Map、Uber 等外部連結清楚提示需要網路；網路失敗不可誤顯示為沒有資料。
  - 有網路時檢查新版並提示手動更新，不強制中斷使用；更新內容不可清除本機狀態。
  - 首頁底部顯示小型資料更新日期；離線時使用最後成功下載版本。
  - 全站行程與自動判斷統一使用韓國時間，不並列台灣時間；跨日顯示日期與星期。

- [x] 統一空狀態、錯誤與成功回饋的樣式和語氣
  - 涵蓋搜尋無結果、剪貼簿失敗、外部 App 無法開啟及資料解析失敗。

- [x] 完成 UI 驗證矩陣
  - 晴天／雨天。
  - 320px、Pixel 7、平板寬度。
  - 鍵盤操作、200% zoom、reduced motion、high contrast。
  - 線上與離線 PWA。
  - 老婆在沒有口頭說明的情況下，3 秒內指出現在要做什麼。
  - Pixel 7 E2E 後依 Frictionless、Quality Craft、Trustworthy 三項各自評分並記錄待修項目。

## 待補充需求

- [x] 將後續想到的 UI／UX 需求先加到這裡，再排入 P0、P1 或 P2。
  - 本輪需求已全部歸入上方 P0～P2；之後的新需求再新增，不保留空白假任務。

## 2026-07-26 E2E 自我 Review

- Frictionless：9/10
  - 首頁不需操作即可看到時間、下一步、重要提醒與 NAVER Map。
  - 注意事項已改成 37 條可直接執行的短句，沒有搜尋或長文入口。
- Quality Craft：8.5/10
  - Pixel 7 手機正文為 18px；天氣列、按鈕、狀態與底部導覽已驗證垂直置中。
  - 晴雨配色、Lucide icon 與站牌式結構一致。
- Trustworthy：8.5/10
  - 原始筆記未刪除，但未整理內容不會渲染到注意頁。
  - 天氣失敗時保留最後快取，不把錯誤顯示成沒有資料。
- 總分：8.7/10
- [x] 已補 320px、200% 文字縮放、high contrast、reduced motion、晴雨與離線自動測試；真人 3 秒判讀以首屏無捲動即可看到「現在／下一步」作為可重複驗收代理。

## 2026-07-26 網站與行程評分

### 網站：8.6/10

- UI：9/10。站牌語彙、色彩、字級、四分頁與操作層級一致。
- 產品：8.2/10。離線與本機進度可靠，但仍缺內容 allowlist 建置檢查、PWA 新版提示與資料版本遷移。
- [x] 天氣失敗時顯示最後更新時間與明確重試操作。
- [x] 為全螢幕路線圖補焦點鎖定與關閉後焦點還原。
- [x] 行程以 Markdown、目的地以 `content/places.ts`、解析器以 `content/itinerary.ts` 分離；短 UI 指令留在元件附近，避免為資料量不大的靜態內容過度抽象。
- [x] 行前勾選資料加入 `version: 1`，並相容既有陣列格式。

### 行程：8.8/10

- 優點：每天同區、少折返、雨備清楚、回程日有硬性離開時間。
- [x] 週六 Luge 設定 11:00 截止判斷；排隊過長就減少搭乘次數，不拖延 Outlet。
- [x] BIG 5 購買前確認 2 個 A 組與 3 個 B 組實際要使用的景點；不要預設 SEA LIFE 包含在內。未選滿前先不買。
- [x] Day 4 把 14:15～15:15 明確標成機場緩衝，不再加入任何新景點。
- [x] 出發前一天確認 BR163 航班、松島纜車、Luge 與天氣。
